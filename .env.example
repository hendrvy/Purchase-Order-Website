# Copy this file to .env at the project root when running `docker-compose up`.
# docker-compose.yml passes these to the postgres and api services.

# --- Postgres container ---
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=purchase_order_db

# --- Go API container (backend/handlers.go DBConnect) ---
# DB_HOST must be the postgres service name from docker-compose, not localhost.
DB_HOST=postgres
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=purchase_order_db
DB_PORT=5432
DB_SSLMODE=disable

SECRET_KEY=change-this-to-a-long-random-string

UPLOAD_FOLDER_PATH=/app/uploads

# --- Email (SMTP) for forgot-password reset links ---
# Using Gmail: enable 2-Step Verification on the Google account, then
# create an "App Password" (https://myaccount.google.com/apppasswords) -
# use that 16-character app password here, NOT the regular account
# password. SMTP_FROM is optional; falls back to SMTP_USER if left blank.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-16-char-app-password
SMTP_FROM=your-email@gmail.com

# --- Frontend ---
# Base URL of the frontend app, used to build the reset-password link
# embedded in the forgot-password email (e.g. FRONTEND_URL/reset-password?token=...).
FRONTEND_URL=http://localhost:5173
